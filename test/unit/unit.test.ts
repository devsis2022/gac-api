describe('Teste exemplo: Esta é a descrição do bloco de testes', () => {
  it('Esta é a descrição do teste unitário', () => {
    const controller = (): boolean => true
    expect(controller()).toBeTruthy()
  })
})
